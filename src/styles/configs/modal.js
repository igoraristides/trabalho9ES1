import { theme } from '../theme';

export const sizes = {
  default: 550,
  small: 350,
  large: 850,
};

export default {
  borderRadius: '4px',
  padding: '20px',
  background: '#fff',
  color: theme.colors.text,
};
